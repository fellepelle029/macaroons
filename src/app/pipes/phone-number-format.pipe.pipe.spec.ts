import { PhoneNumberFormatPipePipe } from './phone-number-format.pipe.pipe';

describe('PhoneNumberFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneNumberFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
