import { ButtonBgDirective } from './button-bg.directive';

describe('ButtonBgDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonBgDirective();
    expect(directive).toBeTruthy();
  });
});
